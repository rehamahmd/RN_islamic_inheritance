import React from 'react';
import { TouchableOpacity, Text } from 'react-native';

export const CalculationButton = () => {
return <TouchableOpacity style={{height:70,backgroundColor:'red'}}>
    <Text>Calculate</Text>
</TouchableOpacity>
}